import Link from 'next/link'; import {getProducts} from '../lib/api'; import {ProductCard} from '../components/ProductCard'; export default async function Home(){const products=await getProducts();return <main><section className="bg-paper border-b"><div className="container py-16"><p className="text-sm text-brand font-semibold">SR ENTERPRISES</p><h1 className="text-4xl font-bold max-w-xl mt-2">Reliable products for everyday business needs.</h1><p className="muted max-w-xl mt-4">Browse our focused catalogue, enquire with confidence, and place orders through a straightforward process.</p><Link href="/products" className="btn inline-block mt-6">Browse products</Link></div></section><section className="container py-12"><div className="flex justify-between items-baseline"><h2 className="text-2xl font-bold">Featured products</h2><Link className="text-sm text-brand" href="/products">View all</Link></div>{products.length?<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mt-6">{products.slice(0,4).map(p=><ProductCard key={p.id} product={p}/>)}</div>:<p className="muted mt-5">Our catalogue is being prepared. Please check back soon.</p>}</section><section className="container border-t py-12 grid md:grid-cols-3 gap-8"><div><h2 className="font-bold">Straightforward ordering</h2><p className="muted text-sm mt-2">Clear pricing and server-validated availability.</p></div><div><h2 className="font-bold">Business focused</h2><p className="muted text-sm mt-2">A professional ordering experience without distraction.</p></div><div><h2 className="font-bold">Need assistance?</h2><p className="muted text-sm mt-2">Contact SR ENTERPRISES for product and order enquiries.</p></div></section></main>}
