'use client'; import {useState} from 'react'; import {base} from '../../lib/api'; export default function Register(){const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${base}/auth/register`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(!r.ok)return setError(d.message||'Unable to register');sessionStorage.setItem('sr_token',d.token);location.href='/account';}return <main className="container max-w-md py-12"><h1 className="text-3xl font-bold">Create account</h1><form onSubmit={submit} className="card p-6 mt-6 space-y-4"><label>Name<input name="name" required className="mt-1"/></label><label>Email<input name="email" type="email" required className="mt-1"/></label><label>Phone<input name="phone" className="mt-1"/></label><label>Password<input name="password" minLength={8} type="password" required className="mt-1"/></label>{error&&<p className="text-red-700 text-sm">{error}</p>}<button className="btn w-full">Create account</button></form></main>}
