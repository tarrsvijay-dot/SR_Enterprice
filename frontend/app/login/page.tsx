'use client'; import {useState} from 'react'; import {base} from '../../lib/api'; import Link from 'next/link'; export default function Login(){const [error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${base}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const data=await r.json();if(!r.ok)return setError(data.message||'Unable to sign in');sessionStorage.setItem('sr_token',data.token);location.href='/account';}return <main className="container max-w-md py-12"><h1 className="text-3xl font-bold">Sign in</h1><form onSubmit={submit} className="card p-6 mt-6 space-y-4"><label>Email<input name="email" type="email" required className="mt-1"/></label><label>Password<input name="password" type="password" required className="mt-1"/></label>{error&&<p className="text-red-700 text-sm">{error}</p>}<button className="btn w-full">Sign in</button></form><p className="text-sm mt-4">New customer? <Link className="text-brand" href="/register">Create an account</Link></p></main>}
