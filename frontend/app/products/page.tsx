import {getProducts} from '../../lib/api'; import {ProductCard} from '../../components/ProductCard'; export const metadata={title:'Products'}; export default async function Products(){const products=await getProducts();return <main className="container py-10"><h1 className="text-3xl font-bold">Products</h1><p className="muted mt-2">Browse our available catalogue.</p><div className="grid md:grid-cols-[220px_1fr] gap-8 mt-8"><aside className="border p-4 h-fit"><p className="font-semibold">Filter</p><label className="text-sm block mt-4">Category</label><select className="mt-1"><option>All categories</option></select></aside><section>{products.length?<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<p className="muted">No products are available yet.</p>}</section></div></main>}
