import Link from 'next/link'; export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main className="container py-12"><h1 className="text-3xl font-bold capitalize">{slug.replaceAll('-',' ')}</h1><p className="muted mt-3">Products in this category can be browsed from the catalogue.</p><Link className="btn inline-block mt-6" href="/products">View products</Link></main>}
