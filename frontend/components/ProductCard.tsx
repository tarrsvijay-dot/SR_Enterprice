import Link from 'next/link'; import type {Product} from '../lib/api'; export function ProductCard({product:p}:{product:Product}){return <article className="card p-4 flex flex-col"><div className="h-36 bg-slate-100 flex items-center justify-center text-slate-400 text-sm">Product image</div><div className="pt-4 flex-1"><p className="text-xs muted">{p.category||'SR ENTERPRISES'}</p><h3 className="font-semibold mt-1">{p.name}</h3><p className="mt-2 font-bold">₹{Number(p.price).toLocaleString('en-IN')}</p><p className="text-sm muted mt-1">{p.stock>0?'Available':'Out of stock'}</p></div><Link className="btn-outline text-center text-sm mt-4" href={`/products/${p.slug}`}>View Product</Link></article>}
